# atualizacao_ceps_pec
Automatização da atualização dos novos CEPS no PEC

Instalar:
winget install -e --id Python.Python.3.13
python --version
pip install playwright
python -m playwright install chromium

Rodar:
python automacao_pec.py
